/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { MusicChallengeService } from "../musicChallenge.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { MusicChallengeCreateInput } from "./MusicChallengeCreateInput";
import { MusicChallenge } from "./MusicChallenge";
import { MusicChallengeFindManyArgs } from "./MusicChallengeFindManyArgs";
import { MusicChallengeWhereUniqueInput } from "./MusicChallengeWhereUniqueInput";
import { MusicChallengeUpdateInput } from "./MusicChallengeUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class MusicChallengeControllerBase {
  constructor(
    protected readonly service: MusicChallengeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: MusicChallenge })
  @nestAccessControl.UseRoles({
    resource: "MusicChallenge",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createMusicChallenge(
    @common.Body() data: MusicChallengeCreateInput
  ): Promise<MusicChallenge> {
    return await this.service.createMusicChallenge({
      data: {
        ...data,

        virtualArtist: data.virtualArtist
          ? {
              connect: data.virtualArtist,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        description: true,
        id: true,
        rewards: true,
        title: true,
        updatedAt: true,

        virtualArtist: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [MusicChallenge] })
  @ApiNestedQuery(MusicChallengeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "MusicChallenge",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async musicChallenges(
    @common.Req() request: Request
  ): Promise<MusicChallenge[]> {
    const args = plainToClass(MusicChallengeFindManyArgs, request.query);
    return this.service.musicChallenges({
      ...args,
      select: {
        createdAt: true,
        description: true,
        id: true,
        rewards: true,
        title: true,
        updatedAt: true,

        virtualArtist: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: MusicChallenge })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MusicChallenge",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async musicChallenge(
    @common.Param() params: MusicChallengeWhereUniqueInput
  ): Promise<MusicChallenge | null> {
    const result = await this.service.musicChallenge({
      where: params,
      select: {
        createdAt: true,
        description: true,
        id: true,
        rewards: true,
        title: true,
        updatedAt: true,

        virtualArtist: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: MusicChallenge })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MusicChallenge",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateMusicChallenge(
    @common.Param() params: MusicChallengeWhereUniqueInput,
    @common.Body() data: MusicChallengeUpdateInput
  ): Promise<MusicChallenge | null> {
    try {
      return await this.service.updateMusicChallenge({
        where: params,
        data: {
          ...data,

          virtualArtist: data.virtualArtist
            ? {
                connect: data.virtualArtist,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          description: true,
          id: true,
          rewards: true,
          title: true,
          updatedAt: true,

          virtualArtist: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: MusicChallenge })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MusicChallenge",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteMusicChallenge(
    @common.Param() params: MusicChallengeWhereUniqueInput
  ): Promise<MusicChallenge | null> {
    try {
      return await this.service.deleteMusicChallenge({
        where: params,
        select: {
          createdAt: true,
          description: true,
          id: true,
          rewards: true,
          title: true,
          updatedAt: true,

          virtualArtist: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
