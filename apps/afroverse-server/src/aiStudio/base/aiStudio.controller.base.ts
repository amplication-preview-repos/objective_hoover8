/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { AiStudioService } from "../aiStudio.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AiStudioCreateInput } from "./AiStudioCreateInput";
import { AiStudio } from "./AiStudio";
import { AiStudioFindManyArgs } from "./AiStudioFindManyArgs";
import { AiStudioWhereUniqueInput } from "./AiStudioWhereUniqueInput";
import { AiStudioUpdateInput } from "./AiStudioUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class AiStudioControllerBase {
  constructor(
    protected readonly service: AiStudioService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: AiStudio })
  @nestAccessControl.UseRoles({
    resource: "AiStudio",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createAiStudio(
    @common.Body() data: AiStudioCreateInput
  ): Promise<AiStudio> {
    return await this.service.createAiStudio({
      data: {
        ...data,

        virtualArtist: data.virtualArtist
          ? {
              connect: data.virtualArtist,
            }
          : undefined,
      },
      select: {
        composition: true,
        createdAt: true,
        id: true,
        mixing: true,
        production: true,
        updatedAt: true,

        virtualArtist: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [AiStudio] })
  @ApiNestedQuery(AiStudioFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AiStudio",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async aiStudios(@common.Req() request: Request): Promise<AiStudio[]> {
    const args = plainToClass(AiStudioFindManyArgs, request.query);
    return this.service.aiStudios({
      ...args,
      select: {
        composition: true,
        createdAt: true,
        id: true,
        mixing: true,
        production: true,
        updatedAt: true,

        virtualArtist: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: AiStudio })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AiStudio",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async aiStudio(
    @common.Param() params: AiStudioWhereUniqueInput
  ): Promise<AiStudio | null> {
    const result = await this.service.aiStudio({
      where: params,
      select: {
        composition: true,
        createdAt: true,
        id: true,
        mixing: true,
        production: true,
        updatedAt: true,

        virtualArtist: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: AiStudio })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AiStudio",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateAiStudio(
    @common.Param() params: AiStudioWhereUniqueInput,
    @common.Body() data: AiStudioUpdateInput
  ): Promise<AiStudio | null> {
    try {
      return await this.service.updateAiStudio({
        where: params,
        data: {
          ...data,

          virtualArtist: data.virtualArtist
            ? {
                connect: data.virtualArtist,
              }
            : undefined,
        },
        select: {
          composition: true,
          createdAt: true,
          id: true,
          mixing: true,
          production: true,
          updatedAt: true,

          virtualArtist: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: AiStudio })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AiStudio",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteAiStudio(
    @common.Param() params: AiStudioWhereUniqueInput
  ): Promise<AiStudio | null> {
    try {
      return await this.service.deleteAiStudio({
        where: params,
        select: {
          composition: true,
          createdAt: true,
          id: true,
          mixing: true,
          production: true,
          updatedAt: true,

          virtualArtist: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
